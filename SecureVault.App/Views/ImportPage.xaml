<Page
    x:Class="SecureVault.App.Views.ImportPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer>
        <StackPanel Padding="24" Spacing="24" MaxWidth="800">
            <StackPanel Spacing="8">
                <TextBlock Text="Importa Password" FontSize="32" FontWeight="Bold"/>
                <TextBlock Text="Importa le tue password esistenti da Chrome o Samsung Pass" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="24" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                <StackPanel Spacing="16">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <FontIcon Glyph="&#xE774;" FontSize="32" Foreground="#4285F4"/>
                        <TextBlock Text="Google Chrome" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock TextWrapping="Wrap" Text="Per esportare da Chrome: Apri Chrome → Impostazioni → Password → Esporta password → Salva file CSV"/>
                    <Button Content="Seleziona File CSV Chrome" Click="OnImportChromeClick" Style="{StaticResource AccentButtonStyle}"/>
                </StackPanel>
            </Border>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="24" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                <StackPanel Spacing="16">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <FontIcon Glyph="&#xE8EA;" FontSize="32" Foreground="#1428A0"/>
                        <TextBlock Text="Samsung Pass" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock TextWrapping="Wrap" Text="Per esportare da Samsung Pass: Apri app Samsung Pass → Impostazioni → Esporta dati → Salva file"/>
                    <Button Content="Seleziona File Samsung Pass" Click="OnImportSamsungClick" Style="{StaticResource AccentButtonStyle}"/>
                </StackPanel>
            </Border>

            <Border x:Name="ResultsPanel" Visibility="Collapsed" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="24" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                <StackPanel Spacing="12">
                    <TextBlock Text="Risultati Importazione" FontSize="20" FontWeight="SemiBold"/>
                    <TextBlock x:Name="ResultsText" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>

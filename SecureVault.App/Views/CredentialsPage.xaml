<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SecureVault.App.Views.CredentialsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="My Credentials" 
                   FontSize="32" 
                   FontWeight="Bold"
                   Margin="0,0,0,24"/>

        <!-- Search and Filter -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchBox" Grid.Column="0"
                     PlaceholderText="Search credentials..."
                     Margin="0,0,16,0">
                <TextBox.KeyboardAccelerators>
                    <KeyboardAccelerator Key="Enter" Invoked="OnSearchKeyPressed"/>
                </TextBox.KeyboardAccelerators>
            </TextBox>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <Button Content="All" Click="OnFilterAll" Style="{StaticResource AccentButtonStyle}"/>
                <Button Content="Weak" Click="OnFilterWeak"/>
                <Button Content="Duplicate" Click="OnFilterDuplicate"/>
                <Button Content="Compromised" Click="OnFilterCompromised"/>
            </StackPanel>
        </Grid>

        <!-- Credentials List -->
        <ListView x:Name="CredentialsList" Grid.Row="2"
                  SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="12" Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="48"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Icon -->
                        <Border Grid.Column="0" 
                                Width="40" 
                                Height="40" 
                                CornerRadius="20"
                                Background="{ThemeResource AccentFillColorDefaultBrush}">
                            <FontIcon Glyph="&#xE8D7;"
                                       FontSize="18"
                                       Foreground="White"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>

                        <!-- Info -->
                        <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Title}" 
                                       FontSize="16" 
                                       FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Username}" 
                                       FontSize="14"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>

                        <!-- Status Badge -->
                        <Border Grid.Column="2"
                                Background="{Binding StatusColor}"
                                CornerRadius="4"
                                Padding="8,4"
                                VerticalAlignment="Center">
                            <TextBlock Text="{Binding StatusText}" 
                                       FontSize="12"
                                       Foreground="White"/>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Loading -->
        <ProgressRing x:Name="LoadingRing" Grid.Row="2"
                      IsActive="False"
                      Width="50" 
                      Height="50"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"/>
    </Grid>
</Page>
